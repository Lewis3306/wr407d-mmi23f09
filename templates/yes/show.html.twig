{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block body %}
    <h1>Lieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu.id }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ lieu.ville }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ lieu.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_yes_index') }}">back to list</a>

    <a href="{{ path('app_yes_edit', {'id': lieu.id}) }}">edit</a>

    {{ include('yes/_delete_form.html.twig') }}
{% endblock %}
